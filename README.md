# Dropdown Package

This package provides a simple dropdown toggling function for JavaScript projects.

## Installation

You can install this package via npm:

```bash
npm install @metalsnow/dropdown-package
```

## Usage

To use the showDropdown function, import it into your JavaScript file:

```js
import { showDropdown } from '@metalsnow/dropdown-package';

// Example usage:
const dropdownContainer = document.getElementById('dropdown-container');
showDropdown(dropdownContainer);
```
